<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.1.0">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Needed to receive ACL_CONNECTED on API < 31 and to inspect the connected device. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <!-- Required on API 31+ to inspect the BluetoothDevice carried by ACL_CONNECTED. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ImePickerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="de.viatorus.neo2externalkeyboard.ACTION_SHOW_IME_PICKER" />
            </intent-filter>
        </receiver>

        <!--
          Detects when a Bluetooth keyboard connects and posts a notification that opens the
          system input method picker when tapped. ACL_CONNECTED is a protected system broadcast
          that is exempt from the implicit-broadcast restrictions, so a manifest-declared
          receiver can still be woken up for it.
        -->
        <receiver
            android:name=".BluetoothKeyboardReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".InputDeviceReceiver"
            android:label="@string/app_name"
            android:exported="true">   <!-- required for API 31+, must be true for system input discovery -->

            <intent-filter>
                <action android:name="android.hardware.input.action.QUERY_KEYBOARD_LAYOUTS" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.input.metadata.KEYBOARD_LAYOUTS"
                android:resource="@xml/keyboard_layouts" />
        </receiver>

        <service
            android:name=".Neo2InputMethodService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>
</manifest>
